version: 1.0.0-preview-{build}

configuration: Release

os: Visual Studio 2017

build_script:
  - dotnet restore
  - cd Quartz.RedisJobStore.Async.UnitTest
  - dotnet test
  - cd cd ..\Quartz.RedisJobStore.Async
  - dotnet build -c Release
